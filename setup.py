from setuptools import setup, find_packages

setup(
    name="snekfrontation",
    version="0.0.0",
    description="",
    license="MIT",
    packages=find_packages(),
    install_requires=["pytest>=3.7.2,<4.0.0"],
    dependency_links=[],
)
