from setuptools import setup, find_packages

setup(
    name='snekfrontation',
    version='0.0.0',
    description='',
    license='MIT',
    packages=find_packages(),
    install_requires=[
    ],
    dependency_links=[],
)
